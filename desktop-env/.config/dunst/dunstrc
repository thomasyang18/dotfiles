[global]
    # Font
    font = JetBrains Mono Nerd Font 14 

    # Enable Pango markup
    markup = full

    # Window geometry
    width = 1000
    height = 1200
    padding = 20
    horizontal_padding = 20  # Consistent padding inside the notification

    # Background color with transparency
    frame_color = "#000000cc"  # Background of the notification
    background = "#000000cc"   # Match background to frame

    # Border settings
    frame_width = 0            # No border
    corner_radius = 10         # Rounded corners

    # Position/anchor (center of the screen)
    origin = center
    offset = 0                 # No offset/margin from the center

[urgency_low]
    background = "#000000cc"
    foreground = "#ffffff"     # White text

[urgency_normal]
    background = "#000000cc"
    foreground = "#ffffff"

[urgency_critical]
    background = "#000000cc"
    foreground = "#ffffff"
